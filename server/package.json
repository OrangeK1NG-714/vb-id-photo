{
  "name": "id-photo-server",
  "version": "0.1.0",
  "description": "AI 证件照小程序后端骨架：图像精修 + 换底色 + 微信支付",
  "main": "index.js",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test test/*.test.js",
    "check": "node --check *.js && npm test",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.2.0",
    "sharp": "0.35.3"
  }
}

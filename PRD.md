# 证件照精修小程序 — MVP 产品需求文档（PRD）

> 版本：v0.1 (MVP)
> 定位：**看起来精神的你，但一定过审** —— 精修不出错，不做美颜式改脸。
> 商业闭环：微信小程序 + AI 精修 + 微信支付（单次付费导出高清无水印）

---

## 1. 产品定位

- 一句话：上传自拍/照片，AI 自动抠图换底色、精修（匀肤/去油光/修发丝/调光），裁成标准尺寸，付费导出高清无水印。
- 核心差异化：卡在「美颜相机」和「呆板证件照」中间——**只做合规范围内的精修，不改五官轮廓，保证过审**。
- 目标用户：求职、考证、考公、签证、社保、报名等需要标准证件照且不想跑照相馆的人。
- 需求特征：低频 + 高急迫 + 付费意愿强 → 适合**单次付费**而非订阅。

## 2. 合规红线（产品必须遵守，写进代码约束）

允许（精修）：
- 换纯色背景（白/蓝/红/灰）
- 匀肤、去油光、去痘、轻微提亮
- 修飞起的头发丝、整理毛边
- 光线均衡、白平衡校正
- 自动裁切到标准尺寸、居中对齐

禁止（会导致审核被拒 / 投诉）：
- 瘦脸、大眼、改脸型、拉高
- 改变五官相对位置和轮廓
- 过度磨皮导致失真
- 换表情、换发型

## 3. 功能范围（MVP）

### 3.1 必做（P0）
1. 上传照片（相册 / 拍照）
2. 选择证件照类型（内置尺寸库：一寸、二寸、小一寸、签证、考公等）
3. 选择底色（白 / 蓝 / 红 / 灰）
4. AI 处理：抠图 + 换底 + 精修 + 裁尺寸
5. 预览（带水印）
6. 微信支付
7. 支付成功后返回高清无水印图 + 保存到相册

### 3.2 缓做（P1 / 后续版本）
- 排版打印（一张 6 寸相纸排多张）
- 换正装（西装叠加）
- 历史记录
- 会员/次卡

### 3.3 明确砍掉（本期不做）
- 开放 MCP 给外部 Agent（记入路线图 V2+，MVP 不做）
- 复杂用户体系、社交、分享裂变

## 4. 页面流

```
index（首页：上传 + 选类型 + 选底色）
   │  上传成功 & 选好参数
   ▼
edit（预览：显示带水印结果，可切换底色/尺寸重新处理）
   │  点击「支付并下载」
   ▼
（微信支付弹窗）
   │  支付成功
   ▼
result（结果：高清无水印图 + 保存相册按钮）
```

## 5. 技术架构

```
微信小程序（前端 · 原生）
   │  wx.chooseMedia 选图 → 上传到后端
   ▼
后端服务（Node.js + Express，轻量）
   │  ① 调国产视觉API做人像分割/抠图 + 精修（合规备案要求用国产模型）
   │  ② 换底色、裁标准尺寸、生成带水印预览
   │  ③ 微信支付下单 + 回调验签
   │  ④ 支付成功后发放无水印高清图（临时URL / 加密token）
   ▼
对象存储（存原图/成品图，设置过期自动清理）
```

### 技术选型
- 前端：微信小程序原生（wxml/wxss/js），不引框架，vibe coding 友好
- 后端：Node.js + Express
- AI/视觉：国产人像分割 API（如百度/腾讯/商汤的人像分割 + 图像修复），MVP 可先用一家
- 图像处理：后端用 sharp（裁切/合成/水印）
- 支付：微信支付 JSAPI/小程序支付
- 存储：腾讯云 COS / 阿里云 OSS（设置生命周期自动删原图，保护隐私）

## 6. 资质与成本（立项须知）

- **微信支付需要个体户/企业营业执照**（个人主体开不了商户号）→ 需先办个体户（线上可办，几百元）
- **AI/视觉 API 按次计费**：每张成本约几毛 → 定价 1–6 元/张可覆盖并盈利
- 对象存储 + 服务器：起步每月几十元

## 7. 定价（初版假设，需上线验证）

- 单张高清无水印导出：**3–6 元**
- 可选：看激励视频广告免费导出一次（引流 + 流量主补充收益）
- ⚠️ eCPM、转化率等数字需上线后实测，勿凭空假设

## 8. 隐私与安全（不可省）

- 明确告知：照片仅用于本次生成，X 小时后自动删除
- 原图/成品图存储设过期策略，自动清理
- 传输 HTTPS，接口鉴权
- 不做人脸识别/比对，不留存可识别生物信息用于其他用途

## 9. 里程碑

- 第 1 周：前端三页跑通 + 后端 mock 接口（假图返回），走通全流程 UI
- 第 2 周：接真实抠图/精修 API + 图像合成
- 第 3 周：接微信支付 + 存储 + 隐私清理
- 第 4 周：办资质、提审、灰度、跑第一批真实用户

## 10. 冷启动获客（PM 重点，非技术）

- 微信「搜一搜」SEO：小程序名/简介卡关键词（如「考公证件照」「签证证件照」细分）
- 从一个细分场景切入（别正面刚头部通用证件照），如「考公专用尺寸一键打包」
- 差评区调研头部竞品，把用户吐槽点做成你的卖点

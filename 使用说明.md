# 使用说明

这份说明分为“本地自动验证”和“微信开发者工具联调”。本地通过不等于可以真实收费上线；真实支付、抠图质量和提审仍需要资质与凭证。

## 一、准备环境

- Node.js 22.13 或更高版本，推荐当前 LTS。
- 微信开发者工具。
- 可选 Docker，用于验证生产容器。

确认版本：

```bash
node -v
npm -v
```

## 二、后端自动验证

```bash
cd /Users/mac/Desktop/vb/id-photo-miniprogram/server
npm ci
cp .env.example .env
npm run check
npm run smoke
```

`npm run check` 会做语法检查和全部 Node 内置测试。`npm run smoke` 会自动启动开发服务器、生成测试图、走完处理/显式 mock 支付/受控下载，检查一寸图 `295×413`、六寸排版图 `1200×1800`，然后重启服务并确认订单和下载仍可恢复。

预期稳定输出：

```text
smoke: process/pay/download dimensions ok
smoke: restart persistence ok
```

烟测使用临时目录，结束后自动删除。它只证明开发 mock 流程安全隔离且重启可恢复，不证明真实微信支付或真实人像分割已通过。

## 三、启动开发服务

`.env.example` 已显式设置：

```dotenv
NODE_ENV=development
ALLOW_MOCK_PAYMENTS=true
PUBLIC_BASE_URL=http://localhost:3000
```

启动：

```bash
npm run dev
```

另一个终端检查：

```bash
curl http://localhost:3000/health
```

正常返回：

```json
{"ok":true,"database":"ok","storage":"ok"}
```

如果不想允许开发模拟支付，将 `ALLOW_MOCK_PAYMENTS=false`；此时未配置真实微信支付的下单接口会返回 `PAYMENT_NOT_CONFIGURED`，不会自动放行高清图。

## 四、微信开发者工具联调

1. 导入 `miniprogram/` 目录。
2. 使用真实 AppID 或测试号。
3. 开发者工具本地联调时，在“详情 → 本地设置”勾选“不校验合法域名”。
4. `miniprogram/config.js` 的 `develop` 默认指向 `http://localhost:3000`。
5. 依次走首页选规格 → 上传 → 选底色/精修强度 → 生成预览 → 支付并下载。

开发模式如果没有人像分割和人脸检测配置，结果页前会明确提示降级：此时图片只用于联调，不能当作正式证件照质量，也不宣称头部位置已校验。

真机上的 `localhost` 指手机自身，不是电脑。真机联调需要局域网地址或测试 HTTPS 域名，并加入微信后台合法域名。

## 五、订单恢复和下载

- 结果页会在本地保存最近一次订单号，并从 `GET /api/orders/:orderId` 恢复状态。
- 支付回调尚未到达时可点“刷新支付结果”，不会因为刷新重复扣款。
- 已支付订单可分别保存高清证件照和六寸排版图。
- 高清文件不公开静态托管；未支付返回 402，错误下载令牌返回 403。
- 默认创建两小时后图片会自动删除，结果页会提示重新制作。

## 六、生产配置门槛

生产必须设置 `NODE_ENV=production`、`ALLOW_MOCK_PAYMENTS=false`，并补齐：

- 微信小程序 AppID/Secret。
- 微信支付商户号、32 字节 API v3 密钥、商户私钥、证书序列号、平台证书和 HTTPS 通知地址。
- 国内人像分割 API 地址和密钥。
- HTTPS `PUBLIC_BASE_URL` 与至少 32 字节随机 `DOWNLOAD_SECRET`。
- `miniprogram/config.js` 中 `trial` 和 `release` 的真实 HTTPS 后端地址。

缺少关键生产配置时服务会拒绝启动或支付接口安全拒绝，不会自动降级成可收费状态。

部署、隐私和提审清单见：

- `docs/DEPLOYMENT.md`
- `docs/PRIVACY.md`
- `docs/RELEASE-CHECKLIST.md`

## 七、常见问题

**提示“当前版本尚未配置服务地址”**：`trial` 或 `release` 地址仍为空，先在 `miniprogram/config.js` 填真实 HTTPS 域名。

**提示“支付服务尚未开通”**：mock 已关闭但真实商户配置不完整，这是安全失败，不要把 mock 打开到生产。

**提示“订单尚未支付”**：真实支付回调可能仍在到达，等待几秒后刷新；持续失败时检查回调验签日志和公网通知地址。

**提示照片已删除**：订单超过默认两小时隐私期限，需要重新制作。

**底色没有正确替换**：未配置真实人像分割服务；开发降级只用于跑通流程。

# AGENTS.md — 项目交接与协作说明

> 这份文档给**接手本项目的智能体**看。目的：让你不推翻已定决策、不重复踩坑，直接从「当前状态」往「上线」推进。
> 人类负责人是一位**产品经理**（非专业开发），沟通用中文。凡是需要资质、密钥、付费、对外发布的动作，**先问人，不要自作主张**。

---

## 0. 一句话背景

做一个**微信小程序：AI 证件照精修**，面向国内市场，一次性付费变现（¥6/次，导出无水印高清图 + 六寸排版图）。当前前后端骨架已完成并本地跑通，缺三样才能上线：**营业执照、抠图 API、微信支付真实签名**。

---

## 1. 人类负责人的约束（这决定了所有取舍，别改）

| 维度 | 结论 |
|---|---|
| 目标市场 | **国内中文**（微信生态，不是海外） |
| 变现模式 | **一次性付费 / 睡后收入**（不是订阅） |
| 技术投入 | **纯 vibe coding**——负责人不写代码，靠 agent 产出；方案要尽量少让他手动改代码 |
| 时间 | 一个人**空闲时间**做，别设计成需要持续重运营的重产品 |

---

## 2. 已定产品决策（含原因，**不要重新纠结或推翻**）

1. **品类＝证件照，不是"小红书封面生成器"。**
   原因：证件照在微信「搜一搜」有稳定自然流量、付费意愿强（急需、刚需），生态匹配。封面类用户在小红书，跳微信断链。

2. **定位＝「精修不改五官，面向证件规则」，不是"美颜"。**
   原因：证件照本质需求是**通过审核**。瘦脸/大眼/改脸型会导致审核被拒→用户投诉。差异化正是卡在「太呆板」和「太美颜」中间。
   → 代码红线：`server/imageProcessor.js` 的精修参数刻意克制，**禁止**加瘦脸、放大眼睛、改脸型、换表情。未接真实检测或未完成真图验证时，禁止宣称“保证过审”。

3. **砍掉"开放 MCP 给外部 Agent"。**
   原因：需求真伪存疑（谁会让 Agent 批量生成证件照？）、是独立产品的工作量、国内没有成熟的对外 Agent 计费/鉴权基础设施。已记入路线图 V2+，**MVP 不做**。

4. **必须是全栈（有后端），不能纯前端。**
   原因：① 好的抠图要调第三方 API，key 不能放前端（会被盗刷）；② 微信支付需要后端下单/验签；③ 付费漏斗（免费带水印预览→付费去水印）需要后端控制高清图发放。

5. **UI 风格＝克制、去装饰、像正经微信小程序，不要"AI 味"。**（人类明确要求）
   原因：上一版满屏渐变/光晕/emoji/步骤条被指"一看就是 AI 生成"。已重做为：单主色 `#2E6BE6`、细线分隔、大留白、无渐变/无 emoji/无步骤条、尺寸用列表而非卡片网格。
   → 继续开发 UI 时**遵守这个风格**，别再加装饰性元素。

---

## 3. 当前实现状态（真实，已核对）

### 已完成且本地验证通过 ✅
- 前端三页完整：`index`(选尺寸) → `edit`(选底色/精修强度/生成预览) → `result`(付费下载/存相册)
- 后端四接口：`/api/process`、`/api/pay/create`、`/api/pay/confirm`、`/api/pay/notify`
- 图像流水线：抠图→合成纯色底→轻度精修→按规格裁切→打水印预览→生成六寸排版图
- **已用测试图跑通完整链路**：上传→生成 295×413 带水印预览→模拟支付→拿到无水印高清图 + 1200×1800 排版图，尺寸正确。
- multer 已升级到 2.x（修安全告警）。

### 仍需真实服务 / 外部条件 ⚠️
- **抠图**：未配 API 时只在开发环境明确降级为原图；生产缺少人像分割配置会拒绝启动。
- **微信支付**：V3 请求签名、回调验签解密已经实现，但没有真实商户凭证时无法验证真实收款。开发 mock 只有显式 `ALLOW_MOCK_PAYMENTS=true` 才启用，生产禁止。
- **存储**：订单已用 SQLite 持久化，图片分预览/受保护目录并默认两小时清理；正式多实例部署仍建议换带生命周期的对象存储。
- **人脸检测**：已提供可注入适配器和裁切几何；未配置或服务不可用时会明确降级，不能宣称已满足头部位置规范。

---

## 4. 关键文件地图

```
PRD.md              产品需求文档（完整背景）
README.md           开发者视角说明
使用说明.md          给人类的手把手操作文档
AGENTS.md           本文件

miniprogram/        微信小程序前端（原生 wxml/wxss/js）
  config.js         ★ develop/trial/release 后端地址在这里分环境配置
  app.wxss          全局设计 token（主色/中性色/圆角/按钮/卡片）
  utils/sizes.js    前端展示用规格库 + 底色库
  utils/request.js  网络请求封装（request / uploadImage）
  pages/index/      首页：选规格 + 上传
  pages/edit/       编辑：底色/精修强度 + 调 /api/process 出预览
  pages/result/     结果：调 /api/pay/* 完成支付并下载

server/             Node + Express 后端
  index.js          ★ 组合入口与优雅关闭
  app.js            路由、健康检查、订单恢复和受控下载
  orderStore.js     SQLite 订单状态机
  fileStore.js      预览/受保护文件与安全路径
  paymentService.js 可信支付状态编排
  imageProcessor.js ★ 图像流水线（抠图在这里，未配 API 会降级）
  wxpay.js          ★ 微信支付 V3 签名、通知验签与资源解密原语
  sizes.js          后端权威规格库（导出尺寸以此为准）
  config.js         读 .env
  .env.example      配置模板（复制成 .env）
```

---

## 5. 上线前三道硬门槛（都需要人类先提供东西，**不能自动越过**）

1. **营业执照（个体户即可）** → 才能开微信支付商户号。个人主体开不了。→ **需人类去办**。
2. **抠图 API 的账号和 key** → 选一家人像分割服务（百度/火山/商汤等）。→ **需人类注册并提供 key**。
3. **微信支付商户号 + API v3 密钥 + 商户私钥证书** → 拿到后才能接真实支付。→ **需人类提供凭证**。

> 这三样没到位之前，你能做的只有"本地模拟模式"下的代码完善，**不能真正收费上线**。别假装能跳过。

---

## 6. 给你的任务清单（按优先级；标注是否需要人类先给东西）

### A. 已完成的本地上线准备
- [x] SQLite 订单持久化与状态机。
- [x] `/api/pay/confirm` 只认服务端 `paid` 状态，高清下载要求订单令牌。
- [x] 可注入人脸检测、证件照裁切几何和显式降级。
- [x] 默认两小时幂等图片清理。
- [x] `.gitignore` 覆盖依赖、图片、数据库、环境变量和证书。
- [ ] **UI 细节打磨**：遵守第 2.5 条克制风格；可加"当前选中尺寸打勾"等**功能性**反馈，别加装饰。
- [ ] **错误与边界**：非人像图、多人脸、超大图、网络失败的用户提示。

### B. 需要人类先提供东西才能做（做之前先在对话里问他要）
- [ ] **接真实抠图 API**（要 key）：替换 `imageProcessor.removeBackground` 的真实调用；核算单次成本，确认定价能覆盖。
- [x] **接微信支付 V3 原语**：`wxpay.js` 已实现请求签名、回调验签/解密与幂等状态更新；仍需真实商户凭证完成联调，才能宣称真实收款可用。
- [ ] **换对象存储**（要 COS/OSS 凭证）：图片从本地磁盘迁到对象存储，设生命周期自动清理。
- [ ] **部署上线**（要服务器+域名）：后端部署到有公网 HTTPS 域名的机器，小程序后台配置合法域名，提交审核。

### C. 不要做 / 先问人
- ❌ 不要加瘦脸/大眼/改脸型等功能（违反过审定位）。
- ❌ 不要重新做 MCP（已明确砍掉）。
- ❌ 不要改成订阅制或改品类（已定一次性付费 + 证件照）。
- ❌ 不要把 UI 改回渐变/emoji/花哨风格。
- ⚠️ 任何"对外发布、真实收款、删除已有文件、提交审核"的动作，先问人类确认。

---

## 7. 怎么验证你没把东西改坏

```bash
cd server
npm ci
cp .env.example .env
npm run check
npm run smoke
```
前端：微信开发者工具打开 `miniprogram/`，开发地址由 `miniprogram/config.js` 管理。走一遍首页→编辑→结果，确认降级提示、订单恢复、高清图和排版图保存。

判定标准：改动后上述链路仍然跑通；导出图尺寸与 `server/sizes.js` 一致（如一寸 295×413，六寸排版 1200×1800）。

---

## 8. 坑与注意事项

- **搜索工具够不到国内内容**：竞品差评、微信搜一搜排名、小红书数据等，都需要**人类手动采集**，别用联网搜索假装能拿到。
- **规格库有两份**：`miniprogram/utils/sizes.js`（展示）和 `server/sizes.js`（导出权威）。改尺寸要**两边一起改**，以后端为准。
- **价格有两处**：`server/config.js` 的 `PRICE_FEN`（分）和 `miniprogram/pages/result/result.js` 的 `PRICE`（元），改价两处同步。
- **本地模拟支付必须显式开启**：仅 `develop` + `ALLOW_MOCK_PAYMENTS=true` 可用，**不是真实支付**，别据此认为支付已经上线。
- **成本意识**：抠图 API 按次计费，是真实成本。任何触发它的改动都要考虑成本，别在循环里滥调。

---

## 9. 给接手 agent 的第一步建议

先跑第 7 节的冒烟测试确认现状 → 从第 6.A 节挑「订单持久化」或「图片清理」这类**无需凭证**的任务开始推进 → 遇到需要 key/凭证的任务（6.B），在对话里明确告诉人类"我需要 X 才能继续"，列出他要去哪申请。**不要卡住，也不要假装凭证已具备。**
